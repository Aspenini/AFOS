// Graphics Test Program for AFOS
// This demonstrates the graphics API

// Note: This would need to be compiled with the kernel headers
// and linked as an AFOS executable. For now, we'll create a BASIC version.

// The graphics API functions are:
// - gfx_init(width, height, bpp)
// - gfx_clear(color)
// - gfx_set_pixel(x, y, color)
// - gfx_draw_line(x1, y1, x2, y2, color)
// - gfx_draw_rect(x, y, w, h, color)
// - gfx_fill_rect(x, y, w, h, color)
// - gfx_draw_circle(x, y, radius, color)
// - gfx_fill_circle(x, y, radius, color)
// - gfx_rgb(r, g, b)

